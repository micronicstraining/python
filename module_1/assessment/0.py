# id, type examples
# immutable vs mutable

# everything is an object
help(object)

type(object)
# all objects have types, id and value
# what is the base type of everything?
# go over types none, bool, integer, float, integral, fraction
# string, tuple (namedtuple), list (bool (any, all), integer, mixed), set (frozenset), dictionary (hashing tutorial),
# flow control
# loops & comprehensions
# functions
# classes
# modules
# packages
